# vibe-net

## What is this?

This is a neural network that can recognize a handwritten digit (hardcoded to an ASCII art 7).

## How do I use it?

1. Open `neural_net_viz.html` in your browser.
1. **TODO**: fill out the rest after locking down the UI